"""
model/ package

Each classifier lives in its own file (logistic_regression.py,
decision_tree.py, knn.py, naive_bayes.py, random_forest.py), following the
assignment's required structure. `registry.py` collects them into a single
ordered dict that app.py and train_models.py both use, so there is exactly
one place that defines "the 5 models" for the whole project.
"""
